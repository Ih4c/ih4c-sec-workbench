# Output Contract

The final output must include at least:

- The target request and where the parameters are located
- Which script and which function participate in generating them
- Evidence sources: requests, call stacks, breakpoints, runtime values
- Whether stable reproduction is currently achievable
- If incomplete, which environment gap remains
